---
title: Changelog
type: changelog
lastUpdated: true
data:
  version: 1.3.3
  release_date: 2026-06-29
  title: Clear orphaned ledger entries
  description: Migration rows whose file is absent from the tree can now be folded, clearing the boot blocker a history squash leaves behind.
---

A migration history squash folds the original migration files into the baseline and deletes them from the tree, but the live ledger still carries them as applied. The next `up` then aborts with `applied but absent from the source tree`. This release adds a focused way to reconcile that state.

## Prune orphaned migration entries

`rebase --prune` folds every applied migration row whose file is absent from the source tree, setting `state = 'folded'` and writing a `fold` history row per entry. It touches only orphaned migration entries and leaves pending files untouched, so a following `up` applies the genuinely new migrations with their SQL actually executing.

- Run `reconcile --db` first to confirm the tree still produces the folded migrations' objects against live.
- The fold is reversible: it is recorded in history and reversed by restoring the source file.
- A whole-tree `rebase` stamps pending files as applied without running their SQL, so `--prune` is the correct tool for orphaned entries.
